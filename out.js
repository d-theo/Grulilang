require('./lib/index');

async function main() {
	(await ENV("dev"))
for (const orga of ["ATI"]) {
            for (const property of ["bvbvbv"]) {
            (await CHANGE(orga,property,"rule",(await FILE("./rule.json"))))
    }
    }
}
    main();
    